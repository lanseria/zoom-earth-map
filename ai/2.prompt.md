### history

修复 app\components\SettingsPanel.vue 中当我已经配置视图模式为地球时，重新刷新打开还是平面的问题。

在配置中，添加一个选项，用来是否展示贴图网格，用来调试每个贴图对应的x,y,z以及  SATELLITES id

### history

当前 是 `${props.serverUrl}/himawari/{z}/{y}/{x}/${timestamp}.jpg`
现在改为不同的纬度使用不同的卫星图

卫星	覆盖区域	经度范围
goes-west	东北太平洋 / 美国西部	-180 ~ -112.5
goes-east	美洲 / 大西洋	-135 ~ 0
mtg-zero	欧洲 / 非洲	-45 ~ 45
msg-zero	欧洲 / 非洲（备用）	-45 ~ 45
msg-iodc	中东 / 印度洋	22.5 ~ 90
himawari	亚太 / 澳洲	67.5 ~ 180

### history

给当前的项目中的地图上的每个城市，都添加上支持 hover 效果，比如淡蓝色填充，然后可以点击跳转到对应的 http://bmcr1-wtr-r1:3030/?lat=29.9958&lon=122.2061&name=舟山市

### history

现在的配置设置是不保存在 localStorage 中的，请使用vueuse中的 useLocalStorage 将配置保存在 localStorage 中。

### history

添加一个新的 TimelineControl UI 样式，保留原来的 UI。
新的UI样式类似时间刻度一样可以直接滚动来控制时间，每日刻度最长，小时刻度中，分钟刻度短。
这个样式与原来的大小相同，只是里面的交互逻辑不同。
且可以设置配置

刻度上面的文字不是在中心位置，修复一下，同时支持鼠标中键滚动


### history

移除卫星源中风云4B这个选项，也就移除卫星源。